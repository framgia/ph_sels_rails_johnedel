<% provide(:title, 'Edit Word') %>
<% provide(:button_text, 'Edit Word') %>
<div class="container"><br>
    <div class="card">
        <div class="card-action red lighten-2 white-text">
            <h6>Edit Word</h6>
        </div>
        <div class="form-padding">
            <%= render 'form' %>
        </div>
    </div>
</div>