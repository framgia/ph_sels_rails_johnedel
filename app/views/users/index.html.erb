<% provide(:title, "All users")%>
<div class=container>
    <div class=row>
        <h1> All users </h1>
        <%= will_paginate %>
        <ul class="collection">
            <% @users.each do |user| %>
                <%= render @users %>
            <% end %>
        </ul>
        <%= will_paginate %>
    </div>
</div>